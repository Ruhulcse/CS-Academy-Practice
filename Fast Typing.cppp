#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    cin>>s;
    map<char,int>mp;
    for (char i='a';i<='z';i++)
    {
        int a;
        cin>>a;
        mp[i]=a;
    }
    int ans=0;
    for(int i=0;i<s.length();i++)
    {
        ans+=mp[s[i]];
    }
    cout<<ans;
}
